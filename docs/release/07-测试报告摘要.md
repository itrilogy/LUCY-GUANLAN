# 测试报告摘要

**软件全称：双色球市场分析系统 V1.0**  
**报告日期：2026-08-11**  
**测试类型：功能冒烟 / 单元回归 / 评估烟雾**

---

## 1 测试环境

| 项 | 内容 |
|----|------|
| OS | macOS（开发机） |
| Python | 3.x |
| 依赖 | requirements.txt |
| 数据 | 本地全量历史 + tests/fixtures/mini_draws.json |

---

## 2 自动化测试

命令：

```bash
cd ssq_predictor
python3 -m pytest -q
```

### 2.1 用例范围

| 模块 | 文件 | 覆盖要点 |
|------|------|----------|
| 冒烟 | test_smoke.py | 配置、prize_clamp |
| 验证器 | test_validator_lut.py | LUT 一致性、batch 排序 |
| 软更新 | test_market_soft.py | soft_extend 标签延续 |
| Phase2 | test_phase2.py | 行为/前向/条件反向/排序/实验闸 |

### 2.2 结果（最近一次）

```
14 passed
```

（以发行前再次执行结果为准，可更新本段。）

---

## 3 手工验收清单

| 编号 | 步骤 | 期望 | 结果 |
|------|------|------|------|
| M1 | `make check` | 数据校验通过 + smoke OK | ☐ |
| M2 | `make serve` 打开首页 | 页面加载，品牌与实验室 LOGO 显示 | ☐ |
| M3 | 点击「快速」 | 出现推荐与复式，无未捕获异常 | ☐ |
| M4 | 点击「更新」（可联网） | 成功或优雅失败且服务仍可用 | ☐ |
| M5 | 「保存」后看对照 | 有批次记录 | ☐ |
| M6 | `make eval` | 生成 `data/eval/wf_smoke_latest.json` | ☐ |

---

## 4 评估烟雾（参考）

```bash
make eval
```

示例指标字段：`avg_hit_red_top1`、`hit_vs_random`、`p1c_mae`、`bet_mape`。  
**说明：** hit 接近随机期望属正常，不能作为「可预测」宣传依据。

---

## 5 遗留风险

| 风险 | 等级 | 说明 |
|------|------|------|
| 数据源改版 | 中 | 爬虫可能失效 |
| 并发多 worker | 中 | 官方建议单进程 |
| 实验评分误用 | 低 | 默认关闭并需环境变量 |

---

## 6 结论

在所述环境与用例下，V1.0 **具备发行与软著材料配套条件**。  
正式外发前请完整勾选第 3 节手工清单并更新第 2.2 节结果。

测试执行人：________　审核人：________　日期：________
