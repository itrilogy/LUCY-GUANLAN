# 源程序鉴别材料草稿

由 `05-源程序鉴别材料说明.md` 中的脚本生成：

| 文件 | 说明 |
|------|------|
| `source_head.txt` | 前部核心模块连续源码（约 2890 行） |
| `source_tail.txt` | 后部模块连续源码（约 680 行） |

**下一步：** 导入 Word → 设页眉「双色球市场分析系统 V1.0」与页码 → 约 50 行/页 → 取前 30 页 + 后 30 页 → 导出 PDF。

tail 行数偏少时，可再并入 `engine/data_hub.py` 后半或 `app.py` 后半补足后 30 页。
